import { User, UserAuth, UserData } from "./types";
// import { UserAll } from "./mock.data";
import { ApiLeads, ApiUser } from "@/shared/api/modules";
export class UsersModel {

    //by id user
    // static async getAllUsers(): Promise<Array<Users>> {
    //     return Promise.resolve(UserAll);
    // }
    // static async postAuth(data: UserData): Promise<UserAuth> {
    //     console.log("data model", data);
    //     return ApiUser.getInstance().authUser(data);
    // }
    // static async getUser(): Promise<User> {
    //     const result = ApiUser.getInstance().getUserInfo();
    //     return result;
    // }
}
